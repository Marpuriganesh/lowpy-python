[project]
name = "lowpy-python"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "rich>=15.0.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pytest-clarity>=1.0.1",
    "pytest-sugar>=1.1.1",
]
